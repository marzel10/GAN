For the next meeting you should prepare:
1. MSE for every path and 1 frequency (150 kHz)
2. Plot with the reconstruction of the input signal for an example path 
3. Plot of the HI over RUL for an example path 

Action points after the meeting:
1. Change the loss function into MSE because it is easier to interprate 
2. Implement sparse autoencoder (incorporate into the loss function that as many dim of hte latent space should be 0)
3. For the optimization change the drop rate to integers and then divide by 10 (this is not a priority though)

In order to find the sHI (sub health indicator - HI for each path):
1. Take the latent space from the the AE and feed it to 
    a. another NN (1 layer) that will change it to 1 number
    b. GAN that will connect it with the other paths and produce 1 number per path 
2. Implement the monotonicity loss on the health indicators 


Other optional use of the GAN:
1. Connect every sHI to obtain global HI 

Next action points that would be nice to have for the next meeting: 
1. Proabbility map of the damage on the panel (using the sHI and the Gausian distirbution functions)
    a. Try to first plot it for one path 
    

My current questions/problems (8.01.2026)? 
1. eliptical distribution is more intense (higher probability) for shorter paths 
2. Latent spaces using GAN are not always decreasing 
3. Joined autoencoder - GAN model is to big to be trained (probably), although I feel like it is better right now at predicting latent space 
4. Fully connected 1 path models are good for one path and bad from the other ones -> which are those good and bad paths
5. What loss function to use to allow some paths to be completly not ativated (if healthy)
6. How to adjust the speed of the wave

